新增菜品及其口味功能：提供 POST /admin/dish 接口，Controller 用 DishDTO 同时接收菜品信息和口味列表，Service 的 saveWithFlavor 方法加 @Transactional 保证菜品与口味两张表的数据一致性；先把 DTO 属性拷贝到 Dish 实体并通过 DishMapper.insert 写入 dish 表，利用 useGeneratedKeys 回填自增主键，公共字段 create_time/update_time/create_user/update_user 由已有的 @AutoFill 切面统一填充；拿到菜品 id 后遍历 flavors 为每条口味设置 dish_id，再通过新增的 DishFlavorMapper.insertBatch 用 `<foreach>` 拼成一条 SQL 批量写入 dish_flavor 表，口味为空时跳过插入以避免生成非法 SQL
